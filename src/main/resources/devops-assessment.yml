template: Hello, %s!
defaultName: Stranger

server:
  applicationConnectors:
  - type: http 
    port: 8087
  adminConnectors:
  - type: http
    port: 8088


database:
  # the name of your JDBC driver
  driverClass: com.mysql.jdbc.Driver
  # the username
  user: root
  # the password
  password: strongpassword
  # the JDBC URL
  url: jdbc:mysql://del2vmpldevop03.sapient.com:3306/devops_db
  validationQuery: "SELECT 1 from category"


